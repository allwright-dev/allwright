This directory contains a pinned third-party `chromium-bidi` mapper artifact used by `web-lib`.

- Source package: `chromium-bidi@17.0.2`
- Source artifact path inside the published package: `out/Default/gen/src/mapperTab.js`
- Runtime/build policy: `allwright` uses this checked-in artifact directly and does not require `npm` at build time or runtime

If this artifact is updated in the future, keep the source package version and provenance in this file aligned with the checked-in files.
