{
  "name": "allwright-workspace",
  "private": true,
  "packageManager": "bun@1.2.20",
  "workspaces": [
    "typescript/core",
    "typescript/vitest"
  ],
  "scripts": {
    "build": "bun run build:core && bun run build:vitest",
    "build:core": "bun run --filter @allwright.dev/core build",
    "build:vitest": "bun run --filter @allwright.dev/vitest build",
    "example:playground": "bun run --filter @allwright.dev/core example:playground"
  }
}
