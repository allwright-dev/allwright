import type { LanguageReference } from "../types";

export const rustReference: LanguageReference = {
  id: "rust",
  label: "Rust",
  codeLang: "rust",
  packageName: "allwright",
  installCommand: "cargo add allwright",
  sourceHref: "https://github.com/allwright-dev/allwright/tree/main/rust/allwright",
  modules: [
    {
      slug: "browser",
      title: "Browser",
      description:
        "Launch a Chromium or Firefox session and hold the handle every other call goes through. Nearly every method is async fn returning Result<T, Error> and needs .await; Page is a type alias for Tab (pub type Page = Tab).",
      members: [
        {
          name: "launch_chrome / launch_firefox",
          kind: "function",
          signature: "pub async fn launch_chrome(options: LaunchOptions) -> Result<Browser>\npub async fn launch_firefox(options: LaunchOptions) -> Result<Browser>",
          description: "Launches a Chromium or Firefox session directly. chromium()/firefox() return a BrowserType for the equivalent .launch(options) call.",
          since: "v0.0.14",
          example: "let browser = launch_firefox(LaunchOptions::default()).await?;",
        },
        {
          name: "Browser::page",
          kind: "method",
          signature: "pub fn page(&self) -> Page",
          description: "Returns the browser's initial page. initial_page()/initial_tab() are aliases.",
          since: "v0.0.33",
          example: "let page = browser.page();",
        },
        {
          name: "Browser::new_tab / new_page",
          kind: "method",
          signature: "pub async fn new_tab(&self) -> Result<Tab>\npub async fn new_page(&self) -> Result<Page>",
          description: "Opens a new tab/context in the browser with default options; new_tab_with_options additionally accepts a CommandOptions timeout.",
          since: "v0.0.33",
        },
        {
          name: "Browser::close",
          kind: "method",
          signature: "pub async fn close(&self) -> Result<()>",
          description: "Closes the browser session; idempotent if already closed.",
          since: "v0.0.14",
          example: "browser.close().await?;",
        },
        {
          name: "LaunchOptions",
          kind: "type",
          signature: "pub struct LaunchOptions {\n    pub browser_binary: Option<String>,\n    pub timeout_ms: Option<u32>,\n}",
          description: "Launch-time options: a custom browser binary path and a launch timeout. Implements Default.",
          since: "v0.0.14",
        },
      ],
    },
    {
      slug: "page",
      title: "Page",
      description:
        "Page is Tab under a type alias. Every action here also has a *_with_options twin (e.g. click_with_options) that accepts the same call plus a typed Options struct — shown once here, favoring the fuller signature. Tab::locator(...) and the get_by_* builders under Locator are also implemented directly on Tab.",
      members: [
        {
          name: "navigate / goto",
          kind: "method",
          signature: "pub async fn navigate_with_options(&self, url: impl Into<String>, options: CommandOptions) -> Result<NavigateResult>",
          description: "Navigates the page to a URL; navigate/goto without _with_options uses default options. goto is an alias for navigate.",
          since: "v0.0.14",
          example: "page.navigate(url).await?;",
        },
        {
          name: "click",
          kind: "method",
          signature: "pub async fn click_with_options(&self, css_selector: impl Into<String>, options: CommandOptions) -> Result<ClickResult>",
          description: "Clicks the first matching element; click(...) omits options for the default timeout.",
          since: "v0.0.14",
          example: 'page.click("a[target=_blank]").await?;',
        },
        {
          name: "fill",
          kind: "method",
          signature: "pub async fn fill_with_options(&self, css_selector: impl Into<String>, value: impl Into<String>, options: CommandOptions) -> Result<FillResult>",
          description: "Sets an input/textarea's value; fill(...) omits options for the default timeout.",
          since: "v0.0.14",
        },
        {
          name: "hover",
          kind: "method",
          signature: "pub async fn hover_with_options(&self, css_selector: impl Into<String>, options: CommandOptions) -> Result<ElementResult>",
          description: "Hovers the pointer over the matching element; hover(...) omits options for the default timeout.",
          since: "v0.0.14",
        },
        {
          name: "press",
          kind: "method",
          signature: "pub async fn press_with_options(&self, css_selector: impl Into<String>, key: impl Into<String>, options: PressOptions) -> Result<PressResult>",
          description: "Sends a key press to the matching element, optionally typing PressOptions.text; press(...) omits options.",
          since: "v0.0.14",
        },
        {
          name: "focus",
          kind: "method",
          signature: "pub async fn focus_with_options(&self, css_selector: impl Into<String>, options: CommandOptions) -> Result<ElementResult>",
          description: "Focuses the matching element; focus(...) omits options for the default timeout.",
          since: "v0.0.14",
        },
        {
          name: "highlight",
          kind: "method",
          signature: "pub async fn highlight_with_options(&self, css_selector: impl Into<String>, options: HighlightOptions) -> Result<HighlightResult>",
          description: "Visually highlights matching elements for a duration — a debugging aid; highlight(...) omits options.",
          since: "v0.0.14",
        },
        {
          name: "count",
          kind: "method",
          signature: "pub async fn count_with_options(&self, css_selector: impl Into<String>, options: CommandOptions) -> Result<CountResult>",
          description: "Returns how many elements match the selector; count(...) omits options for the default timeout.",
          since: "v0.0.14",
        },
        {
          name: "text_content / inner_text",
          kind: "method",
          signature: "pub async fn text_content_with_options(&self, css_selector: impl Into<String>, options: CommandOptions) -> Result<TextResult>",
          description: "Reads the raw textContent, or (via inner_text) the rendered innerText, of the matching element.",
          since: "v0.0.14",
          example: "page.text_content(selector).await?;",
        },
        {
          name: "wait_for_selector",
          kind: "method",
          signature: "pub async fn wait_for_selector_with_options(&self, css_selector: impl Into<String>, options: WaitForSelectorOptions) -> Result<WaitForSelectorResult>",
          description: "Waits, retrying, until the selector matches — WaitForSelectorOptions.visible optionally requires visibility.",
          since: "v0.0.14",
          example: "page.wait_for_selector_with_options(selector, WaitForSelectorOptions { visible: Some(true), timeout_ms: Some(10_000) }).await?;",
        },
        {
          name: "screenshot",
          kind: "method",
          signature: "pub async fn screenshot_with_options(&self, options: ScreenshotOptions) -> Result<ScreenshotResult>",
          description: "Captures a PNG screenshot; ScreenshotOptions.full_page captures the whole scrollable page, and .path writes the PNG to disk.",
          since: "v0.0.54 – v0.0.55",
        },
        {
          name: "accessibility_snapshot",
          kind: "method",
          signature: "pub async fn accessibility_snapshot_with_options(&self, options: AccessibilitySnapshotOptions) -> Result<String>",
          description: "Captures the accessibility tree as JSON or YAML text; AccessibilitySnapshotOptions.mode selects Default, Ai (stable aria-ref per node), Autoexpect, or Codegen.",
          since: "v0.1.1",
        },
        {
          name: "locator",
          kind: "method",
          signature: "pub fn locator(&self, css_selector: impl Into<String>) -> Locator",
          description: "Builds a Locator scoped to this page for a CSS/XPath selector, or use the get_by_* semantic builders documented under Locator.",
          since: "v0.0.33",
        },
        {
          name: "register_hook",
          kind: "method",
          signature: "pub async fn register_hook<T: HookType>(&self, hook_type: T) -> Result<Hook<T>>",
          description: "Registers a lifecycle hook before the action that triggers it. HookType is sealed — only the provided NEW_PAGE/FILE_CHOOSER/DOWNLOAD tags implement it.",
          since: "v0.1.5",
        },
        {
          name: "frame_with_options",
          kind: "method",
          signature: "pub async fn frame_with_options(&self, selector: impl Into<String>, options: CommandOptions) -> Result<Tab>",
          description: "Resolves the single iframe matching selector to a page once its document has loaded and settled. Same as page.locator(selector).frame_with_options(options) — see Locator::frame.",
          since: "v0.1.13",
        },
      ],
    },
    {
      slug: "locator",
      title: "Locator",
      description:
        "Locator mirrors Page's action/query methods (click, fill, hover, press, focus, count, text_content, inner_text, wait_for), each delegating to self.page().<method>(self.selector()). What's below is what a Locator adds: building one semantically instead of by raw CSS, and narrowing it once built.",
      members: [
        {
          name: "get_by_role",
          kind: "method",
          signature: "pub fn get_by_role_with_options(&self, role: impl Into<String>, options: RoleOptions) -> Locator",
          description: "Finds elements by ARIA role, with name/exact/checked/disabled/expanded/level/pressed/selected/include_hidden filters. Implemented identically on Tab and Locator, so it can start a query or be chained.",
          since: "v0.1.3",
        },
        {
          name: "get_by_text / get_by_label / get_by_placeholder / get_by_alt_text / get_by_title / get_by_test_id",
          kind: "method",
          signature: "pub fn get_by_text_with_options(&self, text: impl Into<TextMatcher>, options: TextOptions) -> Locator",
          description: "Finds elements by visible text, associated label, placeholder, alt text, title attribute, or test-id. TextMatcher accepts a literal string or a regex+flags.",
          since: "v0.1.3",
        },
        {
          name: "Locator::filter",
          kind: "method",
          signature: "pub fn filter(&self, options: LocatorFilterOptions) -> Locator",
          description: "Narrows matches by has/has_not (nested locator), has_text/has_not_text, and visible. Panics if a nested has/has_not locator belongs to a different page.",
          since: "v0.1.3",
          example: "page.get_by_role(\"listitem\").filter(LocatorFilterOptions { has: Some(page.get_by_role(\"heading\")), ..Default::default() })",
        },
        {
          name: "Locator::not",
          kind: "method",
          signature: "pub fn not(&self, other: &Locator) -> Locator",
          description: "Excludes elements matching other. Panics if other belongs to a different page.",
          since: "v0.1.3",
        },
        {
          name: "Locator::nth / first / last",
          kind: "method",
          signature: "pub fn nth(&self, index: i32) -> Locator\npub fn first(&self) -> Locator\npub fn last(&self) -> Locator",
          description: "Selects one match by position — nth accepts negative indices; first/last are shorthands for nth(0)/nth(-1).",
          since: "v0.1.3",
        },
        {
          name: "Locator::locator",
          kind: "method",
          signature: "pub fn locator(&self, css_selector: impl Into<String>) -> Locator",
          description: "Creates a child locator scoped/chained under this one.",
          since: "v0.0.33",
        },
        {
          name: "Locator::frame",
          kind: "method",
          signature: "pub async fn frame(&self) -> Result<Page>\npub async fn frame_with_options(&self, options: CommandOptions) -> Result<Page>",
          description: "Resolves this iframe — nested or cross-origin — to a normal Page, waiting for exactly one match whose document is complete and has gone 200 ms without DOM mutations. timeout_ms (default 10 000) covers the whole resolution; closing the returned page releases its session without closing the parent tab. Tab::frame_with_options(selector, options) does the same from a page.",
          since: "v0.1.13",
          example: 'let frame = page.locator("#checkout-frame").frame().await?;\nframe.locator("input[name=email]").fill("buyer@example.com").await?;\nlet nested = frame.locator("iframe").frame().await?;',
        },
      ],
    },
    {
      slug: "hooks",
      title: "Hooks",
      description:
        "A hook coordinates a browser-native event that fires asynchronously outside any direct call — a new tab, a native file picker, a download. register_hook before the triggering action, then wait for it, so the event can't be missed between the two. HookType is a sealed trait — NEW_PAGE, FILE_CHOOSER, and DOWNLOAD are the only implementations.",
      members: [
        {
          name: "NEW_PAGE",
          kind: "property",
          signature: "pub const NEW_PAGE: NewPage; // HookType::Output = Tab",
          description: "Waits for a tab opened by the triggering action. Registers on the page that triggers the event (not the browser) as of v0.1.6, which fixed misattribution when more than one tab could plausibly open around the same time.",
          since: "v0.1.5",
          example: "let hook = page.register_hook(allwright::NEW_PAGE).await?;\npage.click(\"a[target=_blank]\").await?;\nlet new_page = hook.wait().await?;",
        },
        {
          name: "FILE_CHOOSER",
          kind: "property",
          signature: "pub const FILE_CHOOSER: FileChooserHook; // HookType::Output = FileChooser",
          description: "Waits for the native file-picker dialog; no OS dialog actually renders. The resulting FileChooser's set_file(...)/set_files(...) supplies one or more paths.",
          since: "v0.1.7",
          example: "let hook = page.register_hook(allwright::FILE_CHOOSER).await?;\npage.click(\"button.open-upload\").await?;\nlet chooser = hook.wait().await?;\nchooser.set_file(\"fixtures/document.pdf\").await?;",
        },
        {
          name: "DOWNLOAD",
          kind: "property",
          signature: "pub const DOWNLOAD: DownloadHook; // HookType::Output = Download",
          description: "Resolves as soon as a download starts, exposing url() and suggested_filename() immediately. The resulting Download's save_as(...) waits for it to finish and copies it to path.",
          since: "v0.1.7",
          example: "let hook = page.register_hook(allwright::DOWNLOAD).await?;\npage.click(\"a.download-report\").await?;\nlet download = hook.wait().await?;\ndownload.save_as(format!(\"artifacts/{}\", download.suggested_filename())).await?;",
        },
      ],
    },
    {
      slug: "mobile",
      title: "Mobile",
      description:
        "Android automation over adb — no Appium, no separate driver server. Reached through the allwright::mobile::android module; iOS has no runtime yet. Android methods take options directly, without a *_with_options split.",
      members: [
        {
          name: "mobile::android::connect",
          kind: "function",
          signature: "pub async fn connect(options: MobileAndroidConnectOptions) -> Result<AndroidDevice>",
          description: "Connects to a device or emulator over adb (by serial, or the first one available) and returns a device handle.",
          since: "v0.0.45 – v0.0.52",
          example: "let device = mobile::android::connect(MobileAndroidConnectOptions { device: Some(\"emulator-5554\".into()), ..Default::default() }).await?;",
        },
        {
          name: "AndroidDevice::launch",
          kind: "method",
          signature: "pub async fn launch(&self, options: MobileAndroidLaunchOptions) -> Result<AndroidApp>",
          description: "Installs (from a local APK path or a URL) and launches an app, resolving the package from app_id or the APK's own metadata.",
          since: "v0.0.45 – v0.0.52",
        },
        {
          name: "AndroidApp::click",
          kind: "method",
          signature: "pub async fn click(&self, selector: &str, options: CommandOptions) -> Result<ClickResult>",
          description: "Taps the matching element.",
          since: "v0.0.45 – v0.0.52",
          example: "app.click(selector, CommandOptions::default()).await?;",
        },
        {
          name: "AndroidApp::fill",
          kind: "method",
          signature: "pub async fn fill(&self, selector: &str, value: &str, options: CommandOptions) -> Result<FillResult>",
          description: "Sets a matching field's value.",
          since: "v0.0.45 – v0.0.52",
        },
        {
          name: "AndroidApp::focus / press",
          kind: "method",
          signature: "pub async fn focus(&self, selector: &str, options: CommandOptions) -> Result<ElementResult>\npub async fn press(&self, selector: &str, key: &str, options: PressOptions) -> Result<PressResult>",
          description: "Focuses a matching element, or sends it a key event.",
          since: "v0.0.57 – v0.0.58",
        },
        {
          name: "AndroidApp::count",
          kind: "method",
          signature: "pub async fn count(&self, selector: &str, options: CommandOptions) -> Result<CountResult>",
          description: "Counts matching elements.",
          since: "v0.0.57 – v0.0.58",
        },
        {
          name: "AndroidApp::text_content / inner_text",
          kind: "method",
          signature: "pub async fn text_content(&self, selector: &str, options: CommandOptions) -> Result<TextResult>",
          description: "Reads text from a matching element.",
          since: "v0.0.57 – v0.0.58",
        },
        {
          name: "AndroidApp::wait_for_selector",
          kind: "method",
          signature: "pub async fn wait_for_selector(&self, selector: &str, options: WaitForSelectorOptions) -> Result<WaitForSelectorResult>",
          description: "Waits, retrying, for an element to appear.",
          since: "v0.0.57 – v0.0.58",
        },
        {
          name: "AndroidApp::screenshot",
          kind: "method",
          signature: "pub async fn screenshot_with_options(&self, options: ScreenshotOptions) -> Result<ScreenshotResult>",
          description: "Captures the app UI as a PNG; a full-page capture stitches several scrolled captures into one image.",
          since: "v0.0.57 – v0.0.58",
        },
        {
          name: "AndroidApp::accessibility_snapshot",
          kind: "method",
          signature: "pub async fn accessibility_snapshot_with_options(&self, options: AccessibilitySnapshotOptions) -> Result<String>",
          description: "Captures a structured accessibility snapshot of the app UI — the same versioned JSON/YAML document shape as the web surface's snapshot.",
          since: "v0.1.4",
        },
        {
          name: "AndroidApp::locator",
          kind: "method",
          signature: "pub fn locator(&self, selector: impl Into<String>) -> AndroidLocator",
          description: "Builds a chainable locator scoped to the app. Android has no get_by_role-style semantic builders — only raw CSS/id=/xpath=/uia= selectors.",
          since: "v0.0.45 – v0.0.52",
        },
      ],
    },
    {
      slug: "config",
      title: "Config",
      description:
        "allwright.config.yaml (or .json) drives browser choice, timeouts, and named profiles without writing it in code. Every client picks the file up the same way.",
      members: [
        {
          name: "resolve_config",
          kind: "function",
          signature: "pub fn resolve_config(options: ResolveConfigOptions) -> Result<ResolvedConfig>",
          description: "Finds/loads the config (optionally scoped to a named suite), merges suite overrides over file defaults, and produces a fully-resolved ResolvedConfig.",
          since: "v0.0.27 – v0.0.30",
        },
        {
          name: "find_config_file / load_config_file",
          kind: "function",
          signature: "pub fn find_config_file(start_dir: impl AsRef<Path>) -> Option<PathBuf>\npub fn load_config_file(config_file: impl AsRef<Path>) -> Result<AllwrightConfig>",
          description: "Walks up from a directory looking for a known config filename, then parses one (JSON or YAML by extension) into an AllwrightConfig.",
          since: "v0.0.27 – v0.0.30",
        },
        {
          name: "launch_configured_browser",
          kind: "function",
          signature: "pub async fn launch_configured_browser(config: &ResolvedConfig) -> Result<Browser>",
          description: "Applies config.server_addr and launches the browser named in config.browser_name with config.launch_options.",
          since: "v0.0.27 – v0.0.30",
        },
        {
          name: "CommandOptions",
          kind: "type",
          signature: "pub struct CommandOptions {\n    pub timeout_ms: Option<u32>,\n}",
          description: "Generic per-call timeout shared by nearly every action across Page/Locator/Mobile. Implements Default.",
          since: "v0.0.14",
        },
        {
          name: "RetryConfig",
          kind: "type",
          signature: "pub struct RetryConfig {\n    pub timeout_ms: Option<u32>,\n    pub interval_ms: Option<u32>,\n}",
          description: "The retry/poll timing policy set in a config file's expect section and merged into ResolvedConfig.expect.",
          since: "v0.0.27 – v0.0.30",
        },
      ],
    },
  ],
};
