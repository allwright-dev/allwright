# Copy to .env.local and fill in to enable Google Analytics (GA4).
# Leave unset to run the site with analytics off (default for local dev).
#
# Measurement ID from GA4 Admin -> Data Streams -> your web stream. Starts with "G-".
NEXT_PUBLIC_GA_ID=
